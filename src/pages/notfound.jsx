import { Layout } from '../components';


// Router.push('/details?name=' + value.nome);
export default () => {

    return (
        <Layout>
            <>
                Ajuste
            </>

        </Layout>
    );
}

